# 系统启动说明

## 重要提示

由于系统需要读取Windows目录的ACL权限信息，**服务器端必须以管理员权限运行**。

## 启动步骤

### 1. 启动服务器（需要管理员权限）

**方式一：使用批处理文件**
1. 找到 `Server\start.bat` 文件
2. **右键点击** → 选择 **"以管理员身份运行"**
3. 在弹出的UAC对话框中点击"是"
4. 服务器将在 http://0.0.0.0:5000 上运行

**方式二：使用PowerShell**
```powershell
# 以管理员身份打开PowerShell
cd E:\Project\PermissionManagementSys\Server
python start_server.py
```

### 2. 启动客户端（不需要管理员权限）

**直接运行：**
- 双击 `start_client.bat`

**或使用PowerShell：**
```powershell
cd E:\Project\PermissionManagementSys
python UIManagement\main_window.py
```

## 功能说明

### 目录管理 - 用户访问权限

点击目录管理中的任意目录后，右侧会显示：

- **目录信息**：名称、路径、共享状态等
- **用户访问权限**：
  - 只显示配置文件（config.json）中的用户
  - 显示每个用户的中文名称和访问权限
  - 权限用颜色区分：
    - 🔴 红色：完全控制
    - 🟡 黄色：写入/删除权限
    - 🟢 绿色：只读权限
    - ⚪ 灰色：其他权限

### 注意事项

1. **服务器必须以管理员权限运行**，否则无法读取目录ACL权限
2. 只显示配置文件中存在的用户（如：lijing、zhanggongan等）
3. 如果目录没有为配置文件中的用户设置权限，会显示提示信息
4. 可以在【权限管理】标签页为用户设置目录访问权限

## 常见问题

### Q: 显示"权限不足"错误？
**A:** 服务器未以管理员权限运行。请关闭服务器，然后以管理员身份重新启动。

### Q: 显示"未找到配置文件中的用户"？
**A:** 该目录的Windows ACL中没有为配置文件中的用户设置权限。请：
   1. 进入【权限管理】标签页
   2. 选择目录和用户
   3. 设置访问权限

### Q: 客户端连接不上服务器？
**A:** 检查：
   1. 服务器是否正常运行
   2. 配置文件中的服务器地址是否正确（config.json）
   3. 防火墙是否阻止了5000端口
